services:
  - type: web
    name: veralux-suite
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    rootDir: .
    envVars:
      - key: PORT
        value: 3000